    const podeDirigir = true
    if(podeDirigir) {
        console.log('Ela pode dirigir')
    }

    const saldoEmConta = 0
    if(!saldoEmConta) {
        console.log('não tem saldo')
    }

    const booComString = "ae hackerzão!!"

    //força o valor a true ou false
    // de acordo com a tabela

    console.log('boolComString', booComString)

    //negação
    console.log('negação', !booComString)

    //negação + forçar a bool
    console.log(

        ! (!!booComString)
        )

    