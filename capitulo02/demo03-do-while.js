// Enquanto a variável não mudar, não para!!

/* let tempoDeParada = true
let contador = 0
while(tempoDeParada) {
    tempoDeParada = contador < 20
    if(contador % 2 === 0) { 
        console.log('Numero par', contador)       
    }
    contador += 1
    // contador = contador + 1
}

// Roda a primeira vez e testa a variável depois!!

do {
    console.log('Só uma vez! Pois o tempo de parada é', tempoDeParada)
} while(tempoDeParada)

while(true) {
    console.log('Nem vao exceutar')
} */


